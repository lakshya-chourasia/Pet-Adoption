## 2024-11-20 - Accessible Icon Buttons in Cards
**Learning:** Icon-only buttons nested within cards often lack accessible names, making them invisible to screen readers.
**Action:** Always pair icon buttons with `aria-label` describing the specific action (e.g., "Add [Name] to favorites") and `title` for mouse users.
