## 2024-07-24 - Icon-Only Button Accessibility Pattern
**Learning:** Found a recurring pattern of icon-only buttons (svg wrapped in button) missing accessible names and hover tooltips for specific items (like favoriting a pet).
**Action:** Always ensure icon-only interactive elements have contextual `aria-label`s for screen readers and `title` attributes for sighted users.
